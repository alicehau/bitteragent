[project]
name = "tinyagent"
version = "0.1.0"
description = "Minimal Python CLI agent"
requires-python = ">=3.11"
dependencies = [
    "click",
    "rich",
    "python-dotenv",
    "anthropic",
]

[project.scripts]
tinyagent = "tinyagent.__main__:cli"
